# To-Do 구조 개편 및 UI/UX 재구성 방안

현재 **회의록 상세 페이지 내부의 To-Do**와 **상단 탭의 글로벌 To-Do 페이지**가 분리되어 있어 사용자 경험(UX)이 파편화되고 중복된 느낌을 주고 있습니다. 이를 해결하기 위해 기능의 역할(Role)을 명확히 분리하고 동선을 매끄럽게 연결하는 구조 개편 방안을 제안합니다.

---

## 🛠 1. 전체 구조 개편 방안 (가장 추천하는 방향)

### [역할 분리] Action Items (회의록) ➔ Task Board (메인) 연동형
가장 표준적이고 직관적인 SaaS(예: Notion, Slack) 방식입니다. 회의록은 '생성'에, 메인 페이지는 '관리'에 집중합니다.

* **회의록 페이지의 변화**
  * 사이드바 명칭을 'To-Do'에서 **'후속 과제 (Action Items)'**로 변경.
  * 복잡한 탭을 없애고 AI가 추출한 과제만 심플하게 노출.
  * `[메인 보드로 보내기]` 버튼을 통해 해당 항목을 글로벌 To-Do 페이지로 전송.
* **메인 To-Do 페이지의 변화**
  * 넘어온 과제들은 모두 이곳에서 통합 관리.
  * 각 과제 카드에는 `출처: 5/12 기획 회의`와 같이 원본 회의록으로 돌아갈 수 있는 링크 태그 부착.

---

## 🎯 2. 메인 To-Do 페이지의 업무 관리/정렬 방식 (View)

To-Do 페이지에 여러 회의에서 쏟아져 나온 업무들이 쌓일 텐데, 이를 어떻게 관리하면 좋을지에 대한 가장 효과적인 3가지 뷰(View) 방안을 추천해 드립니다.

### ① 기본 뷰: 상태별 칸반 보드 (Status Kanban) - 추천 ⭐️
업무의 '흐름'을 파악하는 데 가장 이상적인 방식입니다.
- **구조**: `할 일(To-Do)` ➔ `진행 중(In Progress)` ➔ `완료(Done)` 3개의 세로 단(Column) 구성.
- **장점**: 업무가 얼마나 진척되었는지 직관적으로 볼 수 있고, 카드를 드래그(Drag & Drop)해서 상태를 옮기는 손맛을 줄 수 있습니다.

### ② 그룹화 모드 1: 회의별 묶어보기 (Group by Meeting)
회의록 기반 서비스의 정체성을 가장 잘 살릴 수 있는 뷰입니다.
- **구조**: "UI/UX 주간 회의", "마케팅 킥오프" 등 **회의 제목을 기준**으로 가로 단(Row)이나 섹션을 나눕니다.
- **활용**: "지난주 금요일 기획 회의 때 나온 후속 작업들 다 끝났나?" 하고 회의 단위로 팔로업(Follow-up)할 때 매우 강력합니다.

### ③ 그룹화 모드 2: 우선순위 및 마감일 순 (Priority & Due Date)
실무자가 "지금 당장 뭘 해야 하지?"를 결정할 때 필수적인 뷰입니다.
- **우선순위 순**: `긴급(High)` / `보통(Medium)` / `여유(Low)` 로 섹션을 나누어 급한 불부터 끄도록 유도합니다.
- **마감일 임박 순**: 기한(Due Date)이 오늘이거나 지연된 업무들을 최상단에 붉은색으로 강조합니다.

---

## 💡 최종 UX 제안: "다중 뷰(Multi-View) 토글 시스템"

회의 별, 상태 별, 우선순위 별 방식은 각각 쓰임새가 다릅니다. 따라서 어느 하나만 강제하기보다는, **기본적으로는 '상태별 칸반 보드(①)'를 보여주되, 화면 우측 상단에 `[보기 옵션]` 버튼을 두어 사용자가 원할 때마다 뷰를 전환할 수 있게 만드는 것**이 최적의 UX입니다.

> [!IMPORTANT]
> **Open Question**
> 
> 1. 회의록에서는 단순히 업무를 **"추출해서 메인으로 쏘아 보내는 역할"**만 하고, 세부 관리는 메인 To-Do 페이지에서 하도록 분리하는 방향(1번 구조)에 동의하시나요?
> 2. 메인 To-Do 페이지에서 `상태(진행도)`를 기본으로 보여주되, 상단 필터를 통해 `회의별`, `우선순위별`로 자유롭게 정렬 기준을 바꿀 수 있는 다중 뷰(Multi-View) 방식을 적용하는 것이 어떠신가요?
